export enum ACTIONTYPE {
  REASSIGN = "reassign",
  DELETE = "delete",
}

export enum STATE {
  PUBLISH = "publish",
  UNPUBLISH = "unpublish",
}

export enum TYPE {
  SELF = "self",
  ASSIGN = "assign",
}
export enum CONTEXT {
  USER = "user",
  COHORT = "cohort",
}
export enum STATUS {
  INCOMPLTE = "incomplete",
  COMPLTE = "complete",
  REJETED = "rejected",
}
