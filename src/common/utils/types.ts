export enum ACTIONTYPE {
  REASSIGN = "reassign",
  DELETE = "delete",
}

export enum STATE {
  PUBLISH = "publish",
  DRAFT = "draft",
  ARCHIVED = "archived"

}

export enum TYPE {
  SELF = "self",
  ASSIGN = "assign",
}
export enum CONTEXT {
  USER = "user",
  COHORT = "cohort",
}
export enum STATUS {
  INCOMPLETE = "incomplete",
  COMPLETE = "complete",
  REJECTED = "rejected",
}
